/* MAPO Analyzer — selector de movilizaciones.
   Este módulo NO crea navegación general. navigation_controller.js es el único propietario de Anterior/Siguiente.
   Aquí solo se seleccionan turno y tipo de movilización. */
(function(){'use strict';
 const $=id=>document.getElementById(id),TURNS=['Mañana','Tarde','Noche'];
 const CATS=[['aidedTotal','Movilizaciones totales con ayuda'],['aidedPartial','Movilizaciones parciales con ayuda'],['manualTotal','Movilizaciones totales sin ayuda'],['manualPartial','Movilizaciones parciales sin ayuda']];
 const state=window.MAPOMobilizationSelection||{turn:null,category:null};window.MAPOMobilizationSelection=state;
 const capture=()=>{try{window.captureMobilizations?.()}catch(_){} };
 function menu(){const h=$('formContainer');if(!h)return;h.innerHTML=`<div class="step-title-row"><h3>Tareas de movilización de pacientes</h3><p class="schedule-preview">Seleccione un turno y después una de las cuatro movilizaciones.</p></div><div class="mobilization-navigation"><h4>Seleccione un turno</h4><div class="mobilization-turn-buttons">${TURNS.map((x,i)=>`<button type="button" class="secondary" data-turn="${i}">${x}</button>`).join('')}</div><div id="mobilizationOptions" class="mobilization-options" hidden></div></div>`;h.querySelectorAll('[data-turn]').forEach(b=>b.onclick=()=>categories(+b.dataset.turn));window.MAPONavigation?.bindStandard?.();}
 function categories(turn){state.turn=turn;state.category=null;const b=$('mobilizationOptions');if(!b)return;b.hidden=false;b.innerHTML=`<h4>Turno de ${TURNS[turn]}</h4><div class="mobilization-option-grid">${CATS.map((c,i)=>`<button type="button" class="module-hub-item" data-cat="${i}"><strong>${c[1]}</strong><small>Turno de ${TURNS[turn]}</small></button>`).join('')}</div>`;b.querySelectorAll('[data-cat]').forEach(x=>x.onclick=()=>select(turn,+x.dataset.cat));window.MAPONavigation?.bindStandard?.();}
 function categoryNav(h){h.querySelector('#mobilizationCategoryNav')?.remove();const box=document.createElement('div');box.id='mobilizationCategoryNav';box.className='mobilization-category-nav';box.innerHTML=`<div class="schedule-preview"><strong>Otras movilizaciones del turno de ${TURNS[state.turn]}</strong><div class="actions">${CATS.map((c,i)=>`<button type="button" class="secondary ${i===state.category?'active':''}" data-other-cat="${i}">${c[1]}</button>`).join('')}</div></div>`;h.appendChild(box);box.querySelectorAll('[data-other-cat]').forEach(b=>b.onclick=()=>{capture();state.category=Number(b.dataset.otherCat);renderSelected();});}
 function select(turn,cat){capture();state.turn=turn;state.category=cat;renderSelected();}
 function renderSelected(){if(state.turn===null||state.category===null)return;const h=$('formContainer');if(!h||typeof window.renderMobilizations!=='function')return;window.renderMobilizations(CATS[state.category][0],state.turn);const title=document.createElement('div');title.className='schedule-preview';title.innerHTML=`<strong>${CATS[state.category][1]} — turno de ${TURNS[state.turn]}</strong>`;h.insertBefore(title,h.firstChild);categoryNav(h);const a=document.createElement('div');a.id='mobilizationSubNavigation';a.className='actions';a.innerHTML='<button type="button" class="secondary" id="selectAnotherMobilizationTurn">Seleccionar otro turno</button>';h.appendChild(a);$('selectAnotherMobilizationTurn').onclick=()=>{capture();state.turn=null;state.category=null;menu();};window.MAPONavigation?.bindStandard?.();}
 function isMob(){const d=$('studyDescription');return !!(d&&/Tareas de movilización/i.test(d.textContent||''));}
 function refresh(){if(!isMob())return;if(state.turn===null||state.category===null)menu();else renderSelected();window.MAPONavigation?.bindStandard?.();}
 function hookRender(){if(typeof window.renderStep!=='function'||window.renderStep.__mapoMobHook)return;const original=window.renderStep;function hooked(){original.apply(this,arguments);if(isMob())setTimeout(refresh,0);}hooked.__mapoMobHook=true;window.renderStep=hooked;}
 function init(){hookRender();setTimeout(refresh,0);window.MAPONavigation?.bindStandard?.();}
 if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
 window.MAPOModuleNavigator={refresh,renderMobilizationSelector:menu};
})();